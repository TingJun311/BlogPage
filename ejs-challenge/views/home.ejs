
<head>
    <%- include("partials/header") -%>
        
        <h1 class="mt-3">Home</h1>
        <p><%= content %></p>

        <% postContent.forEach((i) => { %>
            <div>
                <h3><%= i.title %></h3>
                <p><%= i.body %></p>
            </div>
        <% }); %>
    <%- include("partials/footer") -%>
</html>